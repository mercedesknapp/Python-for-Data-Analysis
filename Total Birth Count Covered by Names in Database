import requests
def total_births(file_path):
    total = 0
    with open(file_path, 'r') as file:
        for line in file:
            parts = line.strip().split(',')
            births = int(parts[1])
            total += births
    return total


total_birth_count = total_births(filename)

print(f"The total number of births covered by this list is {total_birth_count}.")

filename = https://github.com/mercedesknapp/Python-for-Data-Analysis-US-Baby-Names/blob/main/USBabyNames2010s.csv
